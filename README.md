# customer-churn-ann
Predict customer churn for bank accounts using a Multi-Layer Perceptron (ANN) implemented in TensorFlow/Keras. The pipeline includes end-to-end data preprocessing, one-hot encoding, feature scaling, dropout regularization, and early stopping.
